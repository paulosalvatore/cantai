---
name: run-app
description: Start the cantai app locally. Stub — the TICKET-1 Dev makes this real.
---

# run-app

STUB — no app exists yet. The TICKET-1 Dev replaces this with the real instructions (expected: `npm install && npm run dev`, Next.js on a dedicated port).
